version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build:exams
  artifacts:
    baseDirectory: apps/exams/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .turbo/**/*
